services:
  - type: web
    name: blackburn-bomber
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -b 0.0.0.0:$PORT"
    envVars:
      - key: PORT
        value: 10000
